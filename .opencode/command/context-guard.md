# /context-guard

Monitor and protect the context window for EitL pipeline agents.

## Usage
```
/context-guard check [agent]
/context-guard compact [agent] [--dry-run]
/context-guard switch-agent <agent>
/context-guard report
```

## Aliases
- /cg — shorthand for /context-guard check auto
